const Effect = require("../../effect/Effect");
const EffectId = require("../../effect/EffectIds");
const {EasyProxy} = require("../../EasyProxy");
const EasyProxyInfo = require("../../EasyProxyInfo");
module.exports = {
    name: "transfer",

    receive(Player, packet)
    {
        /*let address = packet.params.server_address;
        let port = packet.params.port;

        let transferAddress = "127.0.0.1";
        let transferPort = EasyProxyInfo.getDefaultPort();

        let easyProxy=new EasyProxy({
            "address": transferAddress,
            "port": transferPort,
            "version": "1.19.50",

            "destHost": address,
            "destPort": port
        });*/
    }
}